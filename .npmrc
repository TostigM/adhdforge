# Hoist Prisma packages so they're accessible from root scripts and all packages.
# Required for pnpm strict hoisting + Prisma to coexist cleanly.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*
